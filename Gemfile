source "https://rubygems.org"

# Pins Jekyll and all its plugins to the exact versions that
# GitHub Pages uses on its build servers, so what works here
# will also work once published.
gem "github-pages", group: :jekyll_plugins
